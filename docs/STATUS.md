# Status

**2026-09-04** — Initial import of host starter pack (`~/.openclaw/.factory-start-code`) plus planning docs from the openclaw-factory-builder factory coordinator session.

- Scripts are real and used on the OpenClaw host.
- GitHub repo was empty; this is the first content push.
- Foreman naming is canonical in scripts; some live host factories still use legacy `*-factory`.
- Triage role not yet provisioned by skeleton scripts.
